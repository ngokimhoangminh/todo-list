import React from "react";

const Navbar = () => {
    const style = {
      color: "center",
    }

    return (
      <div className="navbar" style={style}>
        <h1>My Hooks App</h1>
      </div>
    );
  };

export default Navbar;
